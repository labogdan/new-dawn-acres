{
  "name": "node-js-sample",
  "version": "0.2.0",
  "description": "A sample Node.js app using Express 4",
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd frontend && ls -la && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.1",
    "node-fetch": "^3.2.9",
    "pg": "^8.7.3"
  },
  "engines": {
    "node": "*"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
